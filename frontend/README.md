# EasyGenerator Frontend

This workspace contains the React + TypeScript UI for the EasyGenerator auth task.

For setup, deployment, and environment variable details, see the root [README.md](../README.md).

Useful commands:

```bash
npm install
npm run dev
npm run build
```
